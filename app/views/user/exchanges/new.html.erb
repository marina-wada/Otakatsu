<div class="container">
  <div class="row">
    <div class="col-xs-8 col-sm-9 col-md-5 text-md-center mt-xs-5 mt-sm-5">
      <%= attachment_image_tag @item, :image, format: 'jpeg', size: "250x250" %>
    </div>

    <div class="col-md-7">
      <div class="row">
        <div class="col-sm-8 col-md-7 mt-sm-2">
          <%= form_with model: @item, local: true do |f| %>
            <div>
              <%= @item.genre.name %>
            </div>
            <div>
              <%= @item.character %>
            </div>
            <div>
              <%= @item.kind %>
            </div>
            <div>
              <%= @item.introduction %>
            </div>
            <div>
              <%= 
              <span><求めるグッズ></span><br>
              <%= f.radio_button :ask_item, 0 %><%= @item.ask_item1 %>
              <div>
                <% if @item.ask_item2.present? %>
                  <%= f.radio_button :ask_item, 1 %><%= @item.ask_item2 %>
                <% end %>
              </div>
              <div>
                <% if @item.ask_item3.present? %>
                  <%= f.radio_button :ask_item, 2 %><%= @item.ask_item3 %>
                <% end %>
              </div>
              <div>
                <% if @item.ask_item4.present? %>
                  <%= f.radio_button :ask_item, 3 %><%= @item.ask_item4 %>
                <% end %>
              </div>
              <div>
                <% if @item.ask_item5.present? %>
                  <%= f.radio_button :ask_item, 4 %><%= @item.ask_item5 %>
                <% end %>
              </div>
            </div>
          <% end %>
        </div>
      </div>
  <div class="row">
    <div class="text-center">
      交換を申し込みますか？<br>
      申し込む場合は交換対象グッズの写真を添付してください。
    </div>
  </div>
  
  <div class="row text-center">
    <%= form_with @item, url: exchange_path(@exchage), local: true do |f| %>
      <div class="col">
        <%= f.attachment_field :image %>
      </div>
      <div class="col">
        <%= f.submit '交換を申し込む' %>
      </div>
  </div>
</div>