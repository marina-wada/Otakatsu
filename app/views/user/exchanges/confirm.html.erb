<div class="container">
  <div class="row">
    <div class="col-xs-8 col-sm-9 col-md-4 text-md-center mt-xs-5 mt-sm-5">
      <%= attachment_image_tag @item, :image, format: 'jpeg', size: "250x250" %>
    </div>

    <div class="col">
      <div class="row">
        <div class="col-xs-12 col-sm-12 col-md-8 mt-sm-2 mt-md-5">
          <div>
            <%= @item.genre.name %>
          </div>
          <div>
            <%= @item.character %>
          </div>
          <div>
            <%= @item.kind %>
          </div>
          <div>
            <%= @item.introduction %>
          </div>
        </div>
      </div>
      <div class="row text-center">
        <div class="col-sm-8 col-md-8 mt-sm-2">
          <p>
            <%= @item.user.nickname %>さんに<%= @ask_item %>にて交換を申し込みますか？<br>
            申し込む場合は交換対象のグッズ写真を添付してください。
          </p>
        </div>
      </div>
      <%= form_with model: @item, url: exchanges_path(@exchage), local: true do |f| %>
        <div class="row">
          <div class="col-xs-9 col-sm-9 col-md-9">
            <%= f.attachment_field :image %>
          </div>
          <div class="col-xs-9 col-sm-9 col-md-9 mt-3">
            <%= f.submit '交換を申し込む', class: "btn btn-success" %>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>