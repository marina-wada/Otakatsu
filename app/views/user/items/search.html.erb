<div class="container">
  <div class="row">
    <div class="lead">
      <% unless params[:keyword].blank? %>
        検索結果"<%= params[:keyword] %>"
      <% else %>
        All Item
      <% end %>
      </div>
  </div>

  <% if @items.count > 0 %>
    <div class="row">
      <% @items.each do |item| %>
        <div class="col-md-3">
          <%= link_to item_path(item.id) do %>
            <%= attachment_image_tag item, :item_image, format: 'jpeg', class: 'search-result-img' %>
            <p>
              <%= item.introduction %>
            </p>
          <% end %>
        </div>
      <% end %>
    </div>
  <% else %>
    Nothing...(T_T)
  <% end %>
</div>